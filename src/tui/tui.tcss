Screen {
    background: $surface;
}

Reverseproxy {
    background: $boost;
    border: hkey;
    height: 20;
    margin-left: 2;
    margin-right: 2;
    margin-top: 1;
}

ClientServerRow > LogContainer {
    background: $boost;
    border: vkey;
    height: 10;
    margin-left: 2;
    margin-right: 2;
    margin-top: 1;
    width: 1fr;
}